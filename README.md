# api_keys_validator
KeyScan v2.0 is a browser-based API key validator that instantly detects the provider, tests the key live against 23 AI APIs, and diagnoses errors — all from a single offline HTML file with no backend or pre-configured credentials needed. It also supports bulk scanning of hundreds of keys at once with CSV export, created by Dhiraj Kumar.
